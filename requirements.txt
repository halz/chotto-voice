# Chotto Voice - Dependencies
# GUI
PyQt6>=6.6.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0

# AI APIs
anthropic>=0.18.0
openai>=1.12.0
google-generativeai>=0.4.0

# Speech-to-text (Whisper API uses openai package)
# For local whisper later: openai-whisper

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Hotkey support
keyboard>=0.13.5

# Clipboard (for text input simulation)
pyperclip>=1.8.2

# Audio control (Windows)
pycaw>=20230407; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"

# Startup shortcut (Windows)
pywin32>=306; sys_platform == "win32"

# Build/Package
pyinstaller>=6.0.0
